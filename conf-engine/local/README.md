# Usage

```
./local-conf-engine -svc webapp-1 \
         -endpoints webapp-1 \
         -template traefik-conf/local-svc.yaml.tpl \
         -output-dir ./traefik.conf.d \
         -output-file local-webapp-1.yaml
```
