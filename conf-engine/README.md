# Usage

```
./engine -svc webapp-1 \
         -endpoints webapp-1 \
         -template traefik-conf/local-svc.conf.tpl \
         -output-dir ./traefik.conf.d \
         -output-file local-webapp-1.conf
```
